import styled from 'styled-components';
import { color } from '../../foundation/palette';
import { fontFamily } from '../../foundation/typography';

export const StyledSelect = styled.div`
    font-family: ${fontFamily.headings};
    color: ${color.violet}
`

